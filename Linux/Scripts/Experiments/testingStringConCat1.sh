#!/bin/bash
# The idea for this script, was to have it process
# a list of string data type, and combine them to 
# form one concatinated string with a IFS in between
# each string as a delimitator


fields=
